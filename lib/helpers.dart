import 'package:flutter/material.dart';

enum KeyType {
  ACKey,
  numericKey,
  percentageKey,
  symbolKey,
  equalsKey,
  emptyKey,
  dotKey,
  divisionKey,
  minusKey,
  clearKey
}

const Color orangeColor = Color.fromARGB(255, 255, 116, 51);
const Color blackColor = Colors.black;
const Color whiteColor = Colors.white;
final Color greyShade = Colors.grey.shade700;
